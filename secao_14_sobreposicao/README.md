# Sobreposição, palavra super, anotação override

Sobreposição é quando se implementa um método pertencente a super classe na subclasse. É uma boa prática usar a anotação **@override** no método que iráfazer a sobreposição.

A palavra **super** chama a implantação da superclasse.